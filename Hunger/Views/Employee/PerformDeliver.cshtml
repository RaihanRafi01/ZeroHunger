@model IEnumerable<Hunger.EF.Models.DeliverReq>

@{
    ViewBag.Title = "PerformDeliver";
    Layout = "~/Views/Shared/_LayoutEmp.cshtml";
}

<h2>Perform Deliver</h2>

<table class="table">
    <tr>
        <th>Id</th>
        <th>
           Employee Name
        </th>
        <th>
            Resturant Name
        </th>
        <th>
            Food Quantity
        </th>
        <th>
            Expire Date(Food)
        </th>
        <th>
            Assigning Date
        </th>
        <th>
            Status
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ResName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FoodQty)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ExpDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AssingDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <th><a href="/Employee/CompleteDelevery/@item.Id">Delever</a> </th>
    </tr>
}

</table>
